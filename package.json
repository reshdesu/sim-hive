{
  "name": "sim-hive",
  "version": "0.1.0",
  "private": true,
  "description": "High-performance browser-based life simulation — Rust/Wasm + PlayCanvas + SolidJS",
  "scripts": {
    "build:wasm": ". $HOME/.cargo/env && cd sim-core && wasm-pack build --target web --out-dir pkg",
    "dev": "pnpm build:wasm && pnpm --filter sim-hive-web dev",
    "dev:web": "pnpm --filter sim-hive-web dev",
    "build": "pnpm build:wasm && pnpm --filter sim-hive-web build",
    "clean": "rm -rf sim-core/pkg web/dist web/node_modules",
    "prepare": "git config core.hooksPath .githooks"
  },
  "engines": {
    "pnpm": ">=11"
  }
}
